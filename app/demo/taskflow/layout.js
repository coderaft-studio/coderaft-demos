export const metadata = { title: "Coderaft TaskFlow — Project Management" };
export default function L({ children }) {
  return <div style={{ background: "#f8fafc", color: "#0f172a" }}>{children}</div>;
}
