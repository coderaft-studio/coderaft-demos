export const metadata = { title: "Prime Property — Real Estate Premium" };
export default function L({ children }) { return <div style={{ background: "#0f172a", color: "#f8fafc" }}>{children}</div>; }
