export const metadata = { title: "IronForge Gym — Train Hard, Live Strong" };
export default function L({ children }) {
  return <div style={{ background: "#080808", color: "#f0fdf4" }}>{children}</div>;
}
