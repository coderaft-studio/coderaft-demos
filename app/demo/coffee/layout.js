export const metadata = { title: "Kopiday Café — Specialty Coffee, Every Day" };
export default function L({ children }) {
  return <div style={{ background: "#fdf6ee", color: "#3d2314" }}>{children}</div>;
}
