export const metadata = { title: "Forma Studio — Digital Design Agency" };
export default function L({ children }) { return <div style={{ background: "#fafafa", color: "#0a0a0a" }}>{children}</div>; }
