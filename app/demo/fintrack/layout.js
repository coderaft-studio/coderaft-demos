export const metadata = { title: "FinTrack Studio — Dashboard Keuangan" };
export default function L({ children }) {
  return <div style={{ background: "#060610", color: "#e2e8f0", minHeight: "100vh" }}>{children}</div>;
}
