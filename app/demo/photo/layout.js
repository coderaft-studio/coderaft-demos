export const metadata = { title: "Lumina Studio — Photography & Visual Arts" };
export default function L({ children }) { return <div style={{ background: "#0a0906", color: "#fdf8f3" }}>{children}</div>; }
